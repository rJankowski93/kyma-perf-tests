apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kyma-perf-tests-chart.fullname" . }}-test-connection"
  labels:
    {{- include "kyma-perf-tests-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kyma-perf-tests-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
